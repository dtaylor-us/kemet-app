package com.kemet.core.repository;

import com.kemet.core.domain.FacultyContent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FacultyContentRepository extends JpaRepository<FacultyContent, String> {
}
